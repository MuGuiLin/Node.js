doctype
html(lang="en")
    head
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <title>后台图片上传</title>
        style.
            h1{ text-align: center;}
            form{ width: 500px; margin: auto; padding: 20px 40px; border: 1px solid gray;}
    body
        h1 相册后台管理
        hr
        br
        form(action="http://127.0.0.6:666/upload" enctype="multipart/form-data" method="POST")
            h3 新建相册：
            p 相册名称：
                input(type="text" name="newName")
            br
            
            h3 选择相册：
            p 相册名称：
                    select(name="oldName")
                        option(value="default") -- 请选择 --
                        - each o, i in boxArr 
                            option(data-id="#{i}" value="#{ o }") #{ i +'、'+ o }

            br

            h3 图片上传：
            p 选择图片：<input type="file" multiple name="photo">
            br
            
            p <input type="submit">



